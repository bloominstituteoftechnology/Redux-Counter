const initialFriendsArray = [
  "Bart",
  "Maggie",
  "Lisa",
  "Homer",
  "Marge",
  "Marge"
];

const friendsReducer = (state = initialFriendsArray, action) => {
  return state;
};

export default friendsReducer;
